services:
  linux:
    container_name: media-server-linux
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    ports:
      - "3000:3000"
      - "20000-20020:20000-20020"
    volumes:
      - ./src:/usr/src
      - ./certs:/certs/ssl
      - snapshots_volume:/usr/media
    networks:
      - cerberus
volumes:
  snapshots_volume:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ../../../Snapshots
networks:
  cerberus:
    external: true
